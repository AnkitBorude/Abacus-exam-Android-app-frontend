<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AbacusApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="actionBarStyle">@style/TransparentActionBar</item>
        <!-- Set ActionBar to overlay other content -->
        <item name="windowActionBarOverlay">true</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="TransparentActionBar" parent="Widget.MaterialComponents.ActionBar.Solid">
        <item name="background">@android:color/transparent</item>
        <item name="elevation">0dp</item> <!-- Removes shadow under ActionBar -->
    </style>
</resources>